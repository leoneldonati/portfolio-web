---
interface Props {
  t_name?: string;
  class?: string;
  style?: string;
}

const { t_name, class: className, style } = Astro.props;
---

<h2
  style={style}
  class=`${className} text-[1.5em] font-bold text-white text-clip backdrop-blur`
  transition:name={t_name}
>
  <slot />
</h2>
