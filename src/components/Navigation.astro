<nav>
  <ul>
    <li>
      <a href="#projects">Projects</a>
    </li>
    <li>
      <a href="#about">About</a>
    </li>
    <li>
      <button class="contact-btn">
        Contact
      </button>
    </li>
  </ul>

  <div class="contact-wrapper">
    <ul>
      <li>
        <a 
          href="https://linkedin.com/in/leoneldonati"
          target="_blank"
          title="Go to LinkedIn"
          rel="noreferrer noopener"
        >
        LinkedIn
        </a>
      </li>
      <li>
        <a 
          href="https://t.me/leonel_web"
          target="_blank"
          rel="noreferrer noopener"
          title="Go to Telegram"
        >
        Telegram
        </a>
      </li>
      <li></li>
    </ul>
  </div>
</nav>
<script>
  const $contactWrapper = document.querySelector('.contact-wrapper') as HTMLElement;
  const $contactBtn = document.querySelector('.contact-btn') as HTMLButtonElement;

  let clicked = false;
  function handleClick () {
    clicked = !clicked;
    $contactWrapper.style.opacity = clicked ? '1' : '0';
    $contactWrapper.style.pointerEvents = clicked ? 'auto' : 'none';
  };
  $contactBtn.addEventListener('click', handleClick);

</script>

<style>
  nav {
    position: relative;
  }
  .contact-wrapper {
    background-color: #333;
    border: 2px solid var(--color-2);
    border-radius: .5em;
    position: absolute;
    right: 0;
    opacity: 0;
    pointer-events: none;
    transition: all .2s cubic-bezier(0.075, 0.82, 0.165, 1);
  }
  .contact-wrapper > ul {
    display: flex;
    flex-flow: column;
  }
  button {
    background: var(--color-2);
    cursor: pointer;
    display: block;
    font-size: 1em;
    color: var(--text-color-light);
    border: transparent 1px solid;
    border-radius: 0.1em;
    padding: 0.3em .7em;
    transition: all 0.2s cubic-bezier(0.165, 0.84, 0.44, 1);
  }
  button:hover {
    transform: scale(.95);
  }
  button:active {
    transform: scale(1.05);
  }

  ul {
    display: flex;
    flex-flow: row wrap;
    align-items: center;
    justify-content: center;
    font-size: var(--paragraph-size);
    list-style: none;
  }

  a {
    display: block;
    color: var(--text-color-light);
    text-decoration: none;
    border: transparent 1px solid;
    border-radius: 0.1em;
    padding: 0.3em 1em;
    transition: all 0.2s cubic-bezier(0.165, 0.84, 0.44, 1);
  }
  a:hover {
    border: var(--color-2) 1px solid;
    background-color: var(--color-2);
    color: #fff;
  }
  a:active {
    transform: scale(0.94);
  }
</style>
